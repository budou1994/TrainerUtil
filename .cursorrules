# Python 专家 CursorRules

## 通用最佳实践

### 代码组织
- **描述**：代码应当有清晰的组织结构，模块化分层，并遵循单一职责原则 (SRP)。
- **示例**：
  - 将业务逻辑、数据层和表现层分开，确保每个模块都有单一的职责。
  - 避免巨型函数，保持函数简短且具备可重用性。

### 清晰代码
- **描述**：确保代码清晰易懂，易于维护。
- **示例**：
  - 使用有意义的变量和函数名称，不要使用晦涩的命名。
  - 避免写长且复杂的函数，使用简洁明了的语法。
  - 使用 Python 中的标准库来避免重复发明轮子。

## 性能优化

### 内存管理
- **描述**：优化内存使用，避免内存泄漏。
- **示例**：
  - 使用生成器代替列表来处理大数据量。
  - 在处理大文件时，逐块读取数据而不是一次性加载所有内容。

### 懒加载
- **描述**：延迟加载可以提高应用的响应速度和启动速度。
- **示例**：
  - 使用 `functools.lru_cache` 装饰器进行函数结果缓存，减少重复计算。
  - 在需要时懒加载模块和依赖项。

## 并发与并行

### 异步编程
- **描述**：尽可能使用异步编程提高并发性能。
- **示例**：
  - 利用 `asyncio` 模块进行异步 I/O 操作，避免阻塞。
  - 使用 `aiohttp` 替代传统的同步 HTTP 请求库，提高请求处理效率。

### 多线程与多进程
- **描述**：在 CPU 密集型任务中使用多线程或多进程提高并行处理能力。
- **示例**：
  - 在处理 CPU 密集型任务时使用 `multiprocessing` 模块。
  - 在线程数较多时，使用 `concurrent.futures.ThreadPoolExecutor` 来管理线程池。

## 测试与质量保证

### 单元测试
- **描述**：确保编写充分的单元测试，保证代码质量。
- **示例**：
  - 使用 `unittest` 或 `pytest` 进行单元测试。
  - 确保每个功能都有相应的测试用例覆盖，尤其是关键路径。

### 集成测试
- **描述**：集成测试用于验证多个组件是否正常协作。
- **示例**：
  - 使用 `pytest` 来进行接口层的集成测试。
  - 确保外部 API 和第三方服务的交互经过充分测试。

### 代码覆盖率
- **描述**：保持较高的代码覆盖率，至少 80% 以上。
- **示例**：
  - 使用 `coverage.py` 工具来检查代码覆盖率。
  - 确保测试用例涵盖不同的边界情况和错误路径。

## 版本控制与部署

### Git 最佳实践
- **描述**：使用 Git 进行版本控制时要遵循最佳实践。
- **示例**：
  - 在提交时编写清晰、简洁的 commit 信息。
  - 使用分支策略（如 Git Flow 或 trunk-based development）以保持主分支稳定。
  - 确保每次提交都有经过测试的代码，避免提交“工作进行中”状态的代码。

### CI/CD 流水线
- **描述**：设置自动化的 CI/CD 流程，以提高发布效率和质量。
- **示例**：
  - 使用 GitHub Actions、Jenkins 或 GitLab CI 来构建自动化部署流水线。
  - 确保每次提交都会触发自动化构建和测试，避免集成问题。

## 项目管理

### 敏捷开发
- **描述**：遵循敏捷开发原则，以快速响应需求变化。
- **示例**：
  - 进行迭代式开发，每个 sprint 都发布可交付的功能。
  - 与产品经理、开发团队和质量保证团队保持频繁沟通，确保项目进度和质量。

### 技术债务管理
- **描述**：管理技术债务，定期进行重构。
- **示例**：
  - 定期检查项目的技术债务，并通过重构减少冗余代码和复杂度。
  - 确保解决每个技术债务都不影响项目的进度或质量。

### 文档维护
- **描述**：保持项目文档的完整和更新，以便团队成员和新成员理解系统架构。
- **示例**：
  - 使用 `Sphinx` 或 `MkDocs` 自动生成项目文档。
  - 确保每个模块和功能都有详细的文档说明，包括使用示例。

## EXE 开发能力

### 打包与分发
- **描述**：将 Python 程序打包为 EXE 文件，方便分发和部署。
- **示例**：
  - 使用 `PyInstaller` 或 `cx_Freeze` 将 Python 脚本打包成独立的 EXE 文件。
  - 配置打包工具以避免冗余文件，并减少 EXE 的体积。
  - 确保 EXE 文件在目标操作系统上能够顺利运行，兼容不同版本的 Python 环境。

### 图形界面开发
- **描述**：为 Python 程序开发图形用户界面（GUI），并将其打包成 EXE 文件。
- **示例**：
  - 使用 `Tkinter`、`PyQt` 或 `Kivy` 开发桌面应用程序。
  - 将图形界面与后台逻辑进行无缝集成，并确保应用稳定性。
  - 利用 `PyInstaller` 打包 GUI 程序为可执行文件。

## 互联网学习能力

### 自学能力
- **描述**：保持与行业前沿技术的同步，不断提升自己的技术水平。
- **示例**：
  - 定期阅读 Python 相关的技术书籍、博客和官方文档。
  - 参加在线课程和培训，学习新的编程语言或工具。

### 利用开源资源
- **描述**：充分利用开源社区提供的资源，提升开发效率。
- **示例**：
  - 使用 GitHub、GitLab 上的开源库和框架，以提高开发效率。
  - 参与开源项目，贡献代码，学习并分享最佳实践。

### 跟进技术趋势
- **描述**：关注最新的 Python 版本和相关技术的动态，保持技术敏感性。
- **示例**：
  - 订阅相关的技术新闻网站和邮件列表（如 Python Weekly）。
  - 参加线上技术讲座、研讨会、以及开源社区的讨论。

