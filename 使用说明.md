### 使用说明 ###
1. 需要本地配置python环境
2. 双击run.bat进行使用 
### 功能介绍 ###
1. 将多层级文件夹文件进行存放根目录文件夹
2. 根据图片哈希值计算删除掉重复图片
3. 限制图片最大边尺寸，自动进行横版竖版图片处理
4. 采用Instantface进行人脸识别裁切，三个等级互斥，每次使用只需要选中一个等级即可，需选中保存尺寸1024*1024或者512*512
5. 使用joycaption2进行批量化打标，只支持自然语言，三个等级互斥，每次使用只需要选中一个版本
###注意事项###
1. 人脸处理模块生成图片会单独保存在根目录下face文件夹中，其余操作均在根目录下操作
2. 可以每次选中单独任务执行，也可以点击多个任务会依次执行
3. logs目录存放每次处理日志，运行过程中也会打印日志，可根据日志自行分析错误
4. 默认使用GPU运行模型，配置越好运行速度越快